import './Player.css'

const PlayerDetails = ({ song, currentSongIndex }) => {

    

    return(
        <div className="player-details">
            {/* <div className="details-img">
                <img src={song[currentSongIndex].image} alt="song image" />
            </div>
            <h3 className="details-title">{song[currentSongIndex].title}</h3>
            <h4 className="details-artist">{song[currentSongIndex].featured}</h4> */}
        </div>
    )
}

export default PlayerDetails